# 资源公共路径,需要以 /开头和结尾
VITE_PUBLIC_PATH = '/'

# 是否启用代理
VITE_USE_PROXY = true

# base api
VITE_BASE_API = '/api/v1'

# 开发代理后端地址（含协议与端口），用于 /api/v1 与 /uploads 转发
VITE_PROXY_TARGET = 'http://127.0.0.1:9999'
